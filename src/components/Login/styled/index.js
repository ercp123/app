import styled from 'styled-components';

const LoginWrapper = styled.div`
  display: flex;
  flex-direction: column;
  min-height: 100vh; /* Garante que o componente ocupa pelo menos toda a altura da tela */
`;

export {
    LoginWrapper
}